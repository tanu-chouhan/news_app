class CategoryModel {
  String categoryName;   // UI name
  String image;          // asset image
     // API category

  CategoryModel({
    required this.categoryName,
    required this.image,

  });
}
